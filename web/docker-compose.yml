version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: interview-status-checker
    restart: always
    ports:
      - "4392:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
